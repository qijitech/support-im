package support.im.aop.custom;

import support.im.aop.BaseAdvice;
import support.im.aop.Pointcut;

public class SDKGlobalConfig extends BaseAdvice {
  public SDKGlobalConfig(Pointcut pointcut) {
    super(pointcut);
  }
}
